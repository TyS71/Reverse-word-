def is_prime?(number)
  div = [] 
  i = 1 
  while i <= (number / 2)
    if number % i == 0 
      div << i 
      i += 1 
    else 
      i += 1 
    end 
  end
  return false if div.length >= 2 
  return true 
end
